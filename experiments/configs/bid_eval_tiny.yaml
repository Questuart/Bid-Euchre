experiment_name: bid_eval_tiny

strategy_roster_path: experiments/baselines/teacher_roster_v1.yaml
include_baselines:
  - strict_raiser
  - heuristics
  - artifact_bidder

scenarios:
  - contract_type: null

parameters:
  n_per: 40
  seed: 42
  log_level: hand
